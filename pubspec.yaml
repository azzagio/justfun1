name: dating_app
description: Une application de dating style Tinder

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  
  # Firebase
  firebase_core: ^2.14.0
  firebase_auth: ^4.6.3
  cloud_firestore: ^4.8.2
  firebase_storage: ^11.2.4
  firebase_messaging: ^14.6.4
  google_sign_in: ^6.1.4
  
  # État et navigation
  provider: ^6.0.5
  get_it: ^7.6.0
  go_router: ^9.0.0
  
  # UI/UX
  flutter_card_swiper: ^6.0.0
  cached_network_image: ^3.2.3
  image_picker: ^1.0.0
  flutter_chat_ui: ^1.6.6
  geolocator: ^9.0.2
  lottie: ^2.4.0
  intl: ^0.18.1
  flutter_svg: ^2.0.7
  uuid: ^3.0.7
  timeago: ^3.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/animations/
    - assets/icons/
